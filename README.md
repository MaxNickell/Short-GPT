# Considerations
- may need to change max sequence length based on how large our graphs will be
- may need to change the vocabulary size based on how large of graphs we test
- will need to add a padding token for the inouts that are not the max sequence length